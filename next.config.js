// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nada de especial por enquanto, usará as configurações padrão do Next.js.
  // Garanta que este arquivo está na raiz do seu projeto e é 'next.config.js' (não .ts)
};

module.exports = nextConfig;